services:
  - type: web
    name: latency-virginia
    env: node
    region: virginia
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LOCATION
        value: virginia
